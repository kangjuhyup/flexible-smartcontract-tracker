import useDashboard from "./hooks/dashboard.hook";

const DashboardPage = () => {

    const { connection,handleConnect } = useDashboard();

    return (
        <div>

        </div>
    )
}

export default DashboardPage;